# Configuración automatizada para Azure Jenkins
# No se requieren variables - Todo se genera automáticamente

# Las claves SSH se generan automáticamente usando el provider TLS
# Los recursos se crean con configuración optimizada para suscripción de estudiante 